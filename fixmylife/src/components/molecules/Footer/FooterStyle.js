import { makeStyles } from "@material-ui/styles";

export default makeStyles((theme) => ({
  footer: {
    position: "fixed",
    bottom: 0,
    paddingRight: 10,
    paddingBottom: 10
  },
  tomatoLogo:{
    width: 30,
  }
}));
